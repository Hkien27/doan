@model IEnumerable<SecondHandSharing.Models.Item>
@{
    ViewData["Title"] = "Tin chờ duyệt";
}

<div class="container my-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">
                <i class="bi bi-hourglass-split text-warning me-2"></i>
                Tin chờ duyệt
            </h2>
            <p class="text-muted mb-0">
                Danh sách các tin người dùng đã đăng và đang đợi admin xét duyệt.
            </p>
        </div>

        @if (Model.Any())
        {
            <span class="badge bg-warning text-dark fs-6 px-3 py-2 rounded-pill">
                @Model.Count() tin
            </span>
        }
    </div>

    <!-- Thông báo -->
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>@TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-x-circle-fill me-2"></i>@TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (!Model.Any())
    {
        <!-- Empty state -->
        <div class="text-center py-5 bg-light rounded-4 border border-dashed">
            <div class="display-4 mb-3">😴</div>
            <h5 class="fw-bold mb-2">Hiện không có tin nào chờ duyệt</h5>
            <p class="text-muted mb-0">Khi người dùng đăng tin mới, chúng sẽ xuất hiện tại đây để bạn xử lý.</p>
        </div>
    }
    else
    {
        <div class="table-responsive shadow-sm rounded-4 bg-white p-2">
            <table class="table align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="width:80px;"></th>
                        <th>Tiêu đề</th>
                        <th>Người đăng</th>
                        <th>Giá</th>
                        <th>Ngày đăng</th>
                        <th class="text-end">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    var imgSrc = string.IsNullOrEmpty(item.Image)
                        ? "/images/no-image.png"
                        : item.Image;

                    <tr>
                        <!-- Ảnh nhỏ -->
                        <td>
                            <img src="@imgSrc"
                                 alt="@item.Title"
                                 class="rounded"
                                 style="width:70px; height:70px; object-fit:cover;" />
                        </td>

                        <!-- Tiêu đề + category + trạng thái -->
                        <td>
                            <div class="fw-semibold text-dark">@item.Title</div>
                            <div class="small text-muted">
                                @if (!string.IsNullOrEmpty(item.Category))
                                {
                                    <span class="me-2">
                                        <i class="bi bi-tag"></i> @item.Category
                                    </span>
                                }
                                <span class="badge bg-warning text-dark">
                                    Chờ duyệt
                                </span>
                            </div>
                        </td>

                        <!-- Người đăng -->
                        <td>
                            <div class="fw-semibold">
                                @item.User?.FullName ?? "Không rõ"
                            </div>
                            <div class="small text-muted">
                                @item.User?.Email
                            </div>
                        </td>

                        <!-- Giá -->
                        <td class="fw-bold text-danger">
                            @item.Price.ToString("N0") đ
                        </td>

                        <!-- Ngày đăng -->
                        <td>
                            <div>@item.CreatedAt.ToString("dd/MM/yyyy")</div>
                            <div class="small text-muted">
                                @item.CreatedAt.ToString("HH:mm")
                            </div>
                        </td>

                        <!-- Nút thao tác -->
                        <td class="text-end">
                            <a asp-controller="Item" asp-action="Details"
                               asp-route-id="@item.ItemId"
                               class="btn btn-sm btn-outline-secondary me-2">
                                <i class="bi bi-eye"></i> Xem
                            </a>

                            <form asp-action="ApproveItem"
                                  asp-route-id="@item.ItemId"
                                  method="post"
                                  class="d-inline">
                                <button type="submit"
                                        class="btn btn-sm btn-success"
                                        onclick="return confirm('Duyệt tin này?');">
                                    <i class="bi bi-check-circle"></i> Duyệt
                                </button>
                            </form>

                            <form asp-action="RejectItem"
                                  asp-route-id="@item.ItemId"
                                  method="post"
                                  class="d-inline ms-1">
                                <button type="submit"
                                        class="btn btn-sm btn-outline-danger"
                                        onclick="return confirm('Từ chối tin này?');">
                                    <i class="bi bi-x-circle"></i> Từ chối
                                </button>
                            </form>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
</div>
