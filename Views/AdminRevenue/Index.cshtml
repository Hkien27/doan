@model SecondHandSharing.Models.ViewModels.AdminRevenueViewModel
@{
    ViewData["Title"] = "Quản lý doanh thu";
}

<style>
    /* Header gradient */
    .admin-header {
        background: linear-gradient(135deg, #ffcc00 0%, #ff9900 100%);
        padding: 30px 20px;
        border-radius: 12px;
        margin-bottom: 30px;
        box-shadow: 0 4px 15px rgba(255, 153, 0, 0.2);
    }

    .admin-header h1 {
        color: #222;
        font-weight: 700;
        margin: 0;
        font-size: 32px;
    }

    .admin-header p {
        color: #333;
        margin: 5px 0 0 0;
        font-size: 14px;
    }

    /* Filter form */
    .filter-form {
        background: white;
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        margin-bottom: 25px;
        border-left: 4px solid #ffcc00;
    }

    .filter-form .form-label {
        font-weight: 600;
        color: #222;
        margin-bottom: 8px;
        font-size: 14px;
    }

    .filter-form .form-control {
        border: 1px solid #e0e0e0;
        border-radius: 8px;
        padding: 10px 12px;
        font-size: 14px;
        transition: all 0.3s ease;
    }

    .filter-form .form-control:focus {
        border-color: #ffcc00;
        box-shadow: 0 0 0 3px rgba(255, 204, 0, 0.1);
        outline: none;
    }

    .filter-form .btn-warning {
        background: linear-gradient(135deg, #ffcc00 0%, #ff9900 100%);
        border: none;
        color: #222;
        font-weight: 600;
        border-radius: 8px;
        padding: 10px 20px;
        transition: all 0.3s ease;
        height: 44px;
    }

    .filter-form .btn-warning:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(255, 153, 0, 0.3);
        color: #222;
    }

    /* Stat cards */
    .stat-card {
        background: white;
        padding: 24px;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        margin-bottom: 25px;
        border-top: 4px solid #ffcc00;
        transition: all 0.3s ease;
    }

    .stat-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 20px rgba(255, 153, 0, 0.15);
    }

    .stat-label {
        color: #666;
        font-size: 14px;
        font-weight: 500;
        margin-bottom: 8px;
    }

    .stat-value {
        font-size: 28px;
        font-weight: 700;
        color: #ff9900;
    }

    .stat-sub {
        font-size: 13px;
        color: #777;
    }

    /* Section titles */
    .section-title {
        color: #222;
        font-weight: 700;
        margin: 30px 0 20px 0;
        font-size: 18px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    /* Tables */
    .table-wrapper {
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        margin-bottom: 25px;
    }

    .table {
        margin: 0;
    }

    .table thead {
        background: linear-gradient(135deg, #ffcc00 0%, #ff9900 100%);
        color: #222;
    }

    .table thead th {
        font-weight: 600;
        padding: 16px;
        border: none;
        font-size: 13px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .table tbody td {
        padding: 14px 16px;
        border-color: #f0f0f0;
        font-size: 14px;
    }

    .table tbody tr {
        transition: all 0.2s ease;
        border-bottom: 1px solid #f0f0f0;
    }

    .table tbody tr:hover {
        background-color: #fffbf0;
        box-shadow: inset 0 0 8px rgba(255, 204, 0, 0.1);
    }

    .amount-bold {
        color: #ff9900;
        font-weight: 700;
    }

    .badge-type {
        background: linear-gradient(135deg, #ffcc00 0%, #ff9900 100%);
        color: #222;
        font-weight: 600;
        padding: 5px 10px;
        border-radius: 6px;
        font-size: 12px;
    }

    .empty-state {
        background: white;
        padding: 40px;
        border-radius: 12px;
        text-align: center;
        color: #999;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .empty-state-icon {
        font-size: 48px;
        margin-bottom: 15px;
    }

    .filter-summary {
        font-size: 13px;
        color: #666;
        margin-top: 8px;
    }
</style>

<div class="container my-4">
    <!-- Header -->
    <div class="admin-header">
        <h1>Quản lý doanh thu</h1>
        <p>Theo dõi và phân tích tất cả giao dịch VIP / Đẩy tin trên hệ thống</p>

        <div class="filter-summary">
            @if (Model.SelectedYear.HasValue || Model.SelectedMonth.HasValue || Model.SelectedUserId.HasValue)
            {
                <span><strong>Bộ lọc hiện tại:</strong></span>

                @if (Model.SelectedYear.HasValue)
                {
                    <span> • Năm: @Model.SelectedYear</span>
                }
                @if (Model.SelectedMonth.HasValue)
                {
                    <span> • Tháng: @Model.SelectedMonth</span>
                }
                @if (Model.SelectedUserId.HasValue)
                {
                    <span> • User ID: @Model.SelectedUserId</span>
                }
            }
            else
            {
                <span><strong>Bộ lọc hiện tại:</strong> Tất cả giao dịch</span>
            }
        </div>
    </div>

    <!-- Filter Form -->
    <form method="get" class="filter-form">
        <div class="row g-3">
            <div class="col-md-3">
                <label class="form-label">Năm</label>
                <input type="number"
                       name="year"
                       value="@Model.SelectedYear"
                       class="form-control"
                       placeholder="VD: 2025" />
            </div>

            <div class="col-md-3">
                <label class="form-label">Tháng</label>
                <input type="number"
                       name="month"
                       value="@Model.SelectedMonth"
                       min="1" max="12"
                       class="form-control"
                       placeholder="1-12" />
            </div>

            <div class="col-md-3">
                <label class="form-label">User ID</label>
                <input type="number"
                       name="userId"
                       value="@Model.SelectedUserId"
                       class="form-control"
                       placeholder="ID người dùng" />
            </div>

            <div class="col-md-3 d-flex align-items-end">
                <button type="submit" class="btn btn-warning w-100">
                    Lọc dữ liệu
                </button>
            </div>
        </div>
    </form>

    <!-- Tổng doanh thu -->
    <div class="stat-card">
        <div class="stat-label">Tổng doanh thu (sau khi áp dụng bộ lọc)</div>
        <div class="stat-value">@Model.TotalRevenue.ToString("N0") đ</div>
        <div class="stat-sub">
            Bao gồm tất cả giao dịch mua VIP và Gói đẩy tin.
        </div>
    </div>

    <!-- Doanh thu theo người dùng -->
    <div class="section-title">
        👤 Doanh thu theo người dùng
    </div>

    @if (Model.PerUserRevenue == null || !Model.PerUserRevenue.Any())
    {
        <div class="empty-state">
            <div class="empty-state-icon">📊</div>
            <p>Không có dữ liệu doanh thu cho bộ lọc hiện tại.</p>
        </div>
    }
    else
    {
        <div class="table-wrapper">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>User ID</th>
                        <th>Email</th>
                        <th>Số giao dịch</th>
                        <th>Tổng tiền đã chi</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var u in Model.PerUserRevenue)
                    {
                        <tr>
                            <td><strong>@u.UserId</strong></td>
                            <td>@u.Email</td>
                            <td>
                                <span class="badge" style="background-color:#e0e0e0; color:#333;">
                                    @u.Count
                                </span>
                            </td>
                            <td class="amount-bold">@u.TotalAmount.ToString("N0") đ</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <!-- Danh sách giao dịch -->
    <div class="section-title">
        💳 Danh sách giao dịch chi tiết
    </div>

    @if (Model.Transactions == null || !Model.Transactions.Any())
    {
        <div class="empty-state">
            <div class="empty-state-icon">💳</div>
            <p>Không có giao dịch nào cho bộ lọc hiện tại.</p>
        </div>
    }
    else
    {
        <div class="table-wrapper">
            <div style="overflow-x:auto;">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>User</th>
                            <th>Gói</th>
                            <th>Loại</th>
                            <th>Sản phẩm</th>
                            <th>Số tiền</th>
                            <th>Ngày tạo</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var t in Model.Transactions)
                        {
                            <tr>
                                <td><strong>#@t.TransactionId</strong></td>
                                <td>
                                    @if (t.User != null)
                                    {
                                        @t.User.Email
                                    }
                                    else
                                    {
                                        <span style="color:#999;">(User đã xoá?)</span>
                                    }
                                </td>
                                <td>
                                    @if (t.Package != null)
                                    {
                                        @t.Package.Name
                                    }
                                    else
                                    {
                                        <span style="color:#999;">—</span>
                                    }
                                </td>
                                <td>
                                    <span class="badge-type">
                                        @t.Type
                                    </span>
                                </td>
                                <td>
                                    @if (t.Item != null)
                                    {
                                        @t.Item.Title
                                    }
                                    else
                                    {
                                        <span style="color:#999;">—</span>
                                    }
                                </td>
                                <td class="amount-bold">
                                    @t.Amount.ToString("N0") đ
                                </td>
                                <td style="color:#999; font-size:13px;">
                                    @t.CreatedAt.ToString("dd/MM/yyyy HH:mm")
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }

</div>
